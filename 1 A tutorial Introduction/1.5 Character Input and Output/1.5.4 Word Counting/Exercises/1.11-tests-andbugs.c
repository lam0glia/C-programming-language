#include <stdio.h>

int main()
{
    printf("I would write a input with words, tabs spaces, new lines and repeat it many times. I would find bugs digiting the real and visible of the characters \\t, \\n, \" \" consecutives");
    return 0;
}