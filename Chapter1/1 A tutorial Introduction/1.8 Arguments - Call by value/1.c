#include <stdio.h>

int main()
{
    printf("functions receives a copy of the arguments as parameters. It`s also possible provide address of the variables (pointer). Arrays are always passed as pointer");
}